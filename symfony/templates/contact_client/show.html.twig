{% extends 'base.html.twig' %}

{% block title %}ContactClient{% endblock %}

{% block body %}
    <h1>ContactClient</h1>

    <table class="table">    
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact_client.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ contact_client.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ contact_client.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contact_client.email }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ contact_client.tel }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ contact_client.message }}</td>
            </tr>
        </tbody>        
    </table>

    <a href="{{ path('contact_client_index') }}">back to list</a>

    <a href="{{ path('contact_client_edit', {'id': contact_client.id}) }}">edit</a>

    {{ include('contact_client/_delete_form.html.twig') }}
{% endblock %}

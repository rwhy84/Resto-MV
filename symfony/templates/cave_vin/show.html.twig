{% extends 'base.html.twig' %}

{% block title %}CaveVin
{% endblock %}

{% block body %}
    <h1>CaveVin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cave_vin.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ cave_vin.image }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ cave_vin.titre }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ cave_vin.prix }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ cave_vin.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cave_vin_index') }}"></i>Retour à la liste des vins</a>

<a href="{{ path('cave_vin_edit', {'id': cave_vin.id}) }}">
    <i class="far fa-edit"></i>Modifier</a>

{{ include('cave_vin/_delete_form.html.twig') }}{% endblock %}

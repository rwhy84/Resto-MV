{% extends 'base-admin.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('asset/css/parametresAdmin.css') }}">
{% endblock %}

{% block title %}Parametres Creneau{% endblock %}
{% block h2 %}Gestion des créneaux d'ouverture / fermeture{% endblock %}

{% block body %}

    <!--  <form>
                         CAROUSEL DES MOIS -->

    <div class="carousel slide" data-interval="false" data-ride="pause" id="carouselExampleControls">
        <div class="carousel-inner">

            <div class="topControls">
                <a class="carousel-control-prev" data-slide="prev" href="#carouselExampleControls" role="button">
                    <span aria-hidden="true" class="carousel-control-prev-icon"></span>
                    <span class="sr-only">Previous</span>
                </a>

                <h3></h3>

                <a class="carousel-control-next" data-slide="next" href="#carouselExampleControls" role="button">
                    <span aria-hidden="true" class="carousel-control-next-icon"></span>
                    <span class="sr-only">Next</span>
                </a>

            </div>

            <div class="carousel-item active currentMonthActive" id="currentMonth"></div>

            <div class="carousel-item" id="nextMonth"></div>

        </div>

    </div>

    <div class="hidden" id="BDDRECAP">
        {% for creneau in creneaux %}

            <a href="#" data-midi="{{ creneau.midi }}" data-soir="{{ creneau.soir }}" id="{{ creneau.datecreneau }}"></a>

        {% endfor %}
    </div>

    <!-- FIN CAROUSEL DES MOIS -->

<!-- <input type="hidden" name="identifiantFormulaire"> 

        </form> -->
    <!-- Chargement du code Javascript de la page -->

{% endblock %}


{% block scriptThisPage %}
    <script>
        var routeAjax = "{{ path('ajaxParametres') }}";
    </script>
    <script src="{{ asset('asset/js/parametresAdmin.js') }}"></script>
{% endblock %}

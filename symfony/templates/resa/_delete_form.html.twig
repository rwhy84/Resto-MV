<form method="post" action="{{ path('resa_delete', {'id': resa.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette réservation ?');">
    <input name="_method" type="hidden" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resa.id) }}">
    <button class="btn btnDel">
        <i class="fas fa-trash-alt"></button>
    </form>
